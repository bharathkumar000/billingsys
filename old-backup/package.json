{
  "name": "voice-bilingual-billing",
  "version": "1.0.0",
  "description": "Voice-driven bilingual retail billing software",
  "main": "index.js",
  "scripts": {
    "postinstall": "node download-font.js",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
